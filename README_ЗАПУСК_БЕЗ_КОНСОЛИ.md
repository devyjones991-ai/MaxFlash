# 🚀 Запуск MaxFlash без консоли

## Самый простой способ

**Просто дважды кликни на файл: `ЗАПУСК_БЕЗ_ОКНА.bat`**

Система запустится в фоне без видимого окна консоли.

## Варианты запуска

### 1. Без окна консоли (рекомендуется)

| Файл | Описание |
|------|----------|
| `ЗАПУСК_БЕЗ_ОКНА.bat` | Самый простой - двойной клик |
| `start_hidden.vbs` | VBS скрипт для скрытого запуска |
| `ЗАПУСК_БЕЗ_КОНСОЛИ.vbs` | Альтернативный VBS скрипт |

### 2. С минимизированным окном

| Файл | Описание |
|------|----------|
| `ЗАПУСК_ПРОСТОЙ.bat` | Окно будет минимизировано |

### 3. С консолью (для отладки)

| Файл | Описание |
|------|----------|
| `ЗАПУСК.bat` | С видимым окном консоли |

## Остановка

### Способ 1: Через bat файл
Дважды кликни на: **`ОСТАНОВИТЬ.bat`**

### Способ 2: Через Диспетчер задач
1. Нажми `Ctrl+Shift+Esc`
2. Найди процесс `pythonw.exe` или `python.exe`
3. Заверши процесс

### Способ 3: Через командную строку
```bash
taskkill /F /IM pythonw.exe
```

## Проверка работы

1. Открой браузер: http://localhost:8050
2. Проверь Telegram бота: t.me/MaxFlash_bot
3. Отправь `/start` боту

## Логи

Если что-то не работает, проверь логи:
- `logs/trading_system_*.log` - Основные логи
- `dashboard.log` - Логи dashboard
- `logs/startup_error.log` - Ошибки запуска

## Решение проблем

### Не запускается
1. Убедись, что установлен Python 3.10+
2. Установи зависимости: `pip install -r requirements.txt`
3. Попробуй запуск с консолью: `python start_final.py`

### Порт 8050 занят
- Закрой другое приложение на порту 8050
- Или измени порт в `web_interface/app.py`

### Telegram бот не работает
- Проверь логи в `logs/`
- Запусти тест: `python test_telegram_bot.py`

