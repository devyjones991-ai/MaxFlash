# 🚀 Быстрый старт для партнера

Простая инструкция для запуска MaxFlash Trading System.

## 1️⃣ Установка

```bash
# Клонировать репозиторий
git clone https://github.com/devyjones991-ai/MaxFlash.git
cd MaxFlash

# Установить зависимости
pip install -r requirements.txt
pip install ccxt cachetools
```

## 2️⃣ Настройка API (ОБЯЗАТЕЛЬНО для реальных сигналов!)

### Шаг 1: Получите API ключ на бирже

**Binance:**
1. Зайдите на https://www.binance.com
2. Войдите в аккаунт
3. Перейдите: **Settings → API Management**
4. Создайте новый API ключ
5. Выберите права: **"Read-only"** (достаточно для получения данных и сигналов)
6. Сохраните **API Key** и **Secret Key**

**Важно:** Для торговли нужны права "Enable Trading", но для просмотра сигналов достаточно "Read-only"

### Шаг 2: Создайте файл `.env`

В корне проекта создайте файл `.env`:

**Windows:**
```bash
copy ENV_TEMPLATE.txt .env
```

**Linux/Mac:**
```bash
cp ENV_TEMPLATE.txt .env
```

Или создайте файл `.env` вручную в корне проекта.

### Шаг 3: Заполните `.env`

Откройте файл `.env` и вставьте ваши ключи:

```env
EXCHANGE_NAME=binance
EXCHANGE_API_KEY=ваш_api_ключ_сюда
EXCHANGE_API_SECRET=ваш_api_секрет_сюда
```

**Пример:**
```env
EXCHANGE_NAME=binance
EXCHANGE_API_KEY=abcdef1234567890
EXCHANGE_API_SECRET=9876543210fedcba
```

## 3️⃣ Запуск Dashboard

### Windows:
```bash
cd web_interface
start_dashboard.bat
```

Или просто дважды кликните на файл `start_dashboard.bat` в папке `web_interface`

### Linux/Mac:
```bash
cd web_interface
chmod +x start_dashboard.sh
./start_dashboard.sh
```

## 4️⃣ Открыть в браузере

После запуска откройте:

**http://localhost:8050**

Вы увидите:
- ✅ Графики цен с индикаторами
- ✅ Order Blocks и Fair Value Gaps
- ✅ Volume Profile
- ✅ Активные торговые сигналы
- ✅ Confluence зоны
- ✅ Метрики производительности

## 🎯 Готово!

Теперь вы видите все сигналы в реальном времени!

---

## ❓ Troubleshooting

### Dashboard не запускается
```bash
# Проверьте зависимости
pip list | grep dash
pip list | grep ccxt

# Если нет - установите
pip install dash dash-bootstrap-components ccxt cachetools
```

### Ошибка "No API key"
- Проверьте файл `.env` в корне проекта
- Убедитесь что ключи правильные (без пробелов)
- Проверьте что файл называется именно `.env` (не `.env.txt`)

### Нет данных на графиках
- Проверьте интернет соединение
- Убедитесь что API ключ активен на бирже
- Проверьте права API ключа (нужно "Read-only" минимум)

### Порт 8050 занят
- Измените порт в `web_interface/config.py`:
```python
DASHBOARD_PORT = 8051  # Другой порт
```

---

## 📞 Помощь

Если что-то не работает - создайте Issue на GitHub или свяжитесь с командой.

**Успешной торговли! 🚀**

