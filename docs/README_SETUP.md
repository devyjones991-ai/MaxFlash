# Настройка MaxFlash Trading System

## Перед первым запуском

### 1. Установите зависимости

Запустите `setup_simple.bat` - он автоматически:
- Создаст виртуальное окружение
- Установит все зависимости Python
- Создаст файл `.env` с настройками по умолчанию

### 2. Настройте API ключи

Откройте файл `.env` в корне проекта и добавьте ваши API ключи.

#### Обязательно для торговли:

```env
BINANCE_API_KEY=ваш_ключ
BINANCE_API_SECRET=ваш_секрет
```

**Где получить Binance API:**
1. Зайдите на [binance.com](https://www.binance.com)
2. Профиль → API Management
3. Create API → Spot & Margin Trading
4. Скопируйте API Key и Secret Key

⚠️ **ВАЖНО:** Используйте только права для Spot Trading! Не давайте права на Withdraw!

#### Опционально для Telegram бота:

```env
TELEGRAM_BOT_TOKEN=ваш_токен
```

**Где получить Telegram Bot Token:**
1. Напишите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен бота

#### Опционально для улучшенной детекции:

```env
ETHERSCAN_API_KEY=ваш_ключ
BSCSCAN_API_KEY=ваш_ключ
HONEYPOT_API_KEY=ваш_ключ
```

**Где получить:**
- **Etherscan:** [etherscan.io](https://etherscan.io) → Sign Up → API Keys
- **BscScan:** [bscscan.com](https://bscscan.com) → Sign Up → API Keys
- **Honeypot:** [honeypot.is](https://honeypot.is) → API Keys

### 3. Режимы работы

#### Без API ключей (Paper Trading)
- ✅ Система работает в безопасном режиме
- ✅ Можно тестировать сигналы
- ✅ Базовый детектор скама работает
- ❌ Реальная торговля недоступна
- ❌ Telegram бот не работает

#### С Binance API (Реальная торговля)
- ✅ Автоматическое исполнение сигналов
- ✅ Реальная торговля на Binance Spot
- ⚠️ **Будьте осторожны!** Используйте `BINANCE_TESTNET=true` для тестирования

#### С Telegram Bot
- ✅ Получение сигналов в Telegram
- ✅ Управление подписками
- ✅ Уведомления о сделках

### 4. Безопасность

- 🔒 **НИКОГДА** не публикуйте `.env` файл в Git!
- 🔒 Используйте только необходимые права для API ключей
- 🔒 Для тестирования используйте `BINANCE_TESTNET=true`
- 🔒 Регулярно проверяйте активность API ключей

### 5. Запуск

После настройки `.env` файла:
```bash
START.bat
```

Система автоматически использует ваши ключи из `.env` файла.

## Пример .env файла

```env
# Database
DATABASE_URL=postgresql+asyncpg://maxflash:maxflash_dev@localhost:5432/maxflash
REDIS_URL=redis://localhost:6379/0

# API
DEBUG=true
SECRET_KEY=dev-secret-key-change-in-production

# Binance (обязательно для торговли)
BINANCE_API_KEY=ваш_ключ
BINANCE_API_SECRET=ваш_секрет
BINANCE_TESTNET=true

# Telegram (опционально)
TELEGRAM_BOT_TOKEN=ваш_токен

# External APIs (опционально)
ETHERSCAN_API_KEY=
BSCSCAN_API_KEY=
HONEYPOT_API_KEY=
```

## Помощь

Если возникли проблемы:
1. Проверьте что все ключи указаны правильно
2. Убедитесь что нет лишних пробелов в `.env` файле
3. Для Binance используйте тестовый режим сначала (`BINANCE_TESTNET=true`)



