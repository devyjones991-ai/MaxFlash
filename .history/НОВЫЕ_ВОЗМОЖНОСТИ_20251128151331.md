# 🆕 Новые возможности MaxFlash (последнее обновление)

## 📅 Дата обновления: 24 ноября 2025

### 🐋 Whale Detector - Детектор крупных транзакций

**Модуль:** `ml/whale_detector.py`

Анализирует публичные сделки для выявления крупных транзакций ("китов") и давления покупок/продаж.

**Возможности:**
- Определение крупных сделок (по умолчанию > $100,000)
- Расчет давления покупок/продаж (pressure: -1.0 до 1.0)
- Доминирование китов (whale dominance) - доля крупных сделок от общего объема
- Net flow - разница между объемами покупок и продаж

**Использование:**
```python
from ml.whale_detector import WhaleDetector

detector = WhaleDetector(large_trade_threshold=100000.0)
trades = await exchange.fetch_trades("BTC/USDT", limit=500)
metrics = detector.detect_whales(trades)

print(f"Whale Pressure: {metrics['pressure']}")
print(f"Whale Dominance: {metrics['dominance']}")
```

### 📰 Sentiment Analyzer - Анализ настроений

**Модуль:** `ml/sentiment_analyzer.py`

Анализирует новости из RSS-лент для определения настроений рынка.

**Возможности:**
- Получение новостей из 4 источников (CoinTelegraph, CoinDesk, CryptoPotato, Bitcoin.com)
- Анализ настроений через TextBlob (полярность от -1.0 до 1.0)
- Фильтрация новостей по символам (BTC, ETH, SOL, XRP, BNB)
- Кэширование результатов (15 минут)

**Использование:**
```python
from ml.sentiment_analyzer import SentimentAnalyzer

analyzer = SentimentAnalyzer()
sentiment = await analyzer.get_sentiment_for_symbol("BTC/USDT")

print(f"Sentiment: {sentiment['label']} (score: {sentiment['score']})")
print(f"News Count: {sentiment['news_count']}")
```

### 🤖 Ensemble Model - Ансамблевая модель

**Модуль:** `ml/ensemble_model.py`

Комбинирует предсказания LSTM (глубокое обучение) и Random Forest (традиционное ML).

**Возможности:**
- Обучение обоих моделей на исторических данных
- Комбинирование предсказаний через взвешенное голосование
- Три класса: BUY, SELL, HOLD
- Вероятности для каждого класса

**Использование:**
```python
from ml.ensemble_model import EnsembleSignalGenerator

ensemble = EnsembleSignalGenerator()
prediction = ensemble.predict(ohlcv_df)

print(f"Action: {prediction['action']}")
print(f"Confidence: {prediction['confidence']}")
print(f"Probabilities: {prediction['ensemble_probabilities']}")
```

### 🧠 LSTM Signal Generator - Генератор сигналов на LSTM

**Модуль:** `ml/lstm_signal_generator.py`

Использует LSTM нейросети для предсказания движения цен.

**Возможности:**
- Архитектура: 2 LSTM слоя + Dropout + Dense слои
- Lookback период: 60 свечей (настраивается)
- 6 признаков: OHLCV + объемные индикаторы
- Выход: 3 класса (Buy, Sell, Hold)

### 📦 Новые торговые модули

#### Order Executor (`trading/order_executor.py`)
- Исполнение ордеров с проверкой баланса
- Поддержка market, limit, stop-limit ордеров
- Автоматическая установка SL/TP
- Отслеживание статуса ордеров
- Повторные попытки при ошибках

#### Advanced Risk Manager (`trading/risk_manager.py`)
- Расчет размера позиции через Kelly Criterion
- Лимиты риска на сделку и портфель
- Проверка корреляций между позициями
- Circuit breakers (остановка при дневном лимите убытков)
- Валидация сделок перед исполнением

### 🔌 Интеграция в API

Все новые функции интегрированы в `/api/v1/signals` endpoint:

1. **Whale Metrics** - добавляется к каждому сигналу
   - `whale_pressure`: давление покупок/продаж
   - `whale_dominance`: доминирование китов

2. **Sentiment Analysis** - влияет на confidence сигнала
   - Буст +0.05 если sentiment совпадает с направлением сигнала

3. **Ensemble Prediction** - основа для сигналов
   - Базовый confidence от ансамблевой модели
   - Буст от whale pressure и sentiment

### 📊 Новые скрипты

- `scripts/train_ml_model.py` - обучение ML моделей
- `scripts/backtest_strategy.py` - бэктестинг с ML моделями
- `scripts/verify_installation.py` - проверка установки всех компонентов

### 🔧 Зависимости

Новые зависимости:
- `feedparser>=6.0.10` - для RSS новостей
- `textblob>=0.17.1` - для анализа настроений
- `tensorflow>=2.18.0` - для LSTM
- `keras>=3.0.0` - для построения моделей
- `scikit-learn>=1.5.0` - для Random Forest

### 📝 Пример использования API

```python
import requests

# Получить сигналы с whale и sentiment анализом
response = requests.get("http://localhost:8000/api/v1/signals?symbol=BTC/USDT&timeframe=15m")
signals = response.json()

for signal in signals:
    print(f"Symbol: {signal['symbol']}")
    print(f"Type: {signal['type']}")
    print(f"Confidence: {signal['confidence']}")
    print(f"Whale Pressure: {signal.get('indicators', {}).get('whale_pressure')}")
```

### ⚠️ Примечания

1. **ML модели требуют обучения** - перед использованием необходимо обучить модели на исторических данных
2. **Testnet по умолчанию** - все торговые операции выполняются в тестовом режиме
3. **Кэширование** - sentiment анализ кэшируется на 15 минут для производительности
4. **Whale detection** - требует доступ к публичным сделкам биржи (может быть ограничено для некоторых пар)

### 🚀 Быстрый старт

```bash
# 1. Установить зависимости
pip install feedparser textblob tensorflow scikit-learn

# 2. Проверить установку
python scripts/verify_installation.py

# 3. Обучить модели (опционально)
python scripts/train_ml_model.py --days 365 --epochs 50

# 4. Запустить API
python run_api.py

# 5. Тестировать
curl http://localhost:8000/api/v1/signals?symbol=BTC/USDT
```

---

**Создано:** 28 ноября 2025  
**Версия:** 1.0.0

