# 🚀 Быстрый запуск MaxFlash

## Самый простой способ (рекомендуется)

### Windows
Просто дважды кликните на файл **`run.bat`** в корне проекта!

Или через командную строку:
```bash
run.bat
```

### Linux/Mac
```bash
chmod +x run.sh
./run.sh
```

Или напрямую:
```bash
python3 run.py
```

## Что делает скрипт запуска?

1. ✅ Проверяет версию Python (нужна 3.9+)
2. ✅ Автоматически устанавливает недостающие зависимости
3. ✅ Настраивает пути для импорта
4. ✅ Запускает dashboard на http://localhost:8050

## Альтернативные способы запуска

### Через web_interface
```bash
cd web_interface
python app_simple.py
```

### Через API (FastAPI)
```bash
cd api
uvicorn main:app --reload
```
API будет доступен на http://localhost:8000
Документация: http://localhost:8000/docs

## Решение проблем

### Ошибка "Python не найден"
Установите Python 3.9+ с https://www.python.org/downloads/

### Ошибка установки зависимостей
Попробуйте вручную:
```bash
pip install --upgrade pip
pip install -r requirements.txt
```

### Порт 8050 занят
Измените порт в файле `web_interface/app.py`:
```python
app.run_server(debug=True, host='0.0.0.0', port=8051)  # Измените на другой порт
```

## Что дальше?

После запуска откройте в браузере:
- **Dashboard**: http://localhost:8050
- **API Docs**: http://localhost:8000/docs (если запущен API)

---

**Всё готово! 🎉**

